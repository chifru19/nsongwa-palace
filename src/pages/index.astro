---
// Import your components here if needed
---

<header>
  <img src="/assets/logo.png" alt="Nsongwa Palace Logo" />
  </header>

<section>
  <iframe src="YOUR_YOUTUBE_URL"></iframe>
  <div class="donation-section">...</div>
</section>

<div class="gallery-container" style="padding: 20px;"> 
  <hr />
  <h3>Jubilee Collection</h3>
  <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 15px;">
    {Object.keys(import.meta.glob('/public/assets/images/jubilee/*.{jpg,jpeg,png}', {eager: true})).map(path => (
      <img src={path.replace('/public', '/nsongwa-palace')} alt="Jubilee" style="width: 100%; border-radius: 8px;" />
    ))}
  </div>

  <h3>Construction Progress</h3>
  <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 15px;">
    {Object.keys(import.meta.glob('/public/assets/images/construction/*.{jpg,jpeg,png}', {eager: true})).map(path => (
      <img src={path.replace('/public', '/nsongwa-palace')} alt="Construction" style="width: 100%; border-radius: 8px;" />
    ))}
  </div>
</div>
